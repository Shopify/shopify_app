class CreateSessions < ActiveRecord::Migration[<%= rails_migration_version %>]
  def self.up
    create_table :sessions do |t|
      t.bigint :shopify_session_id, null: false
      t.string :shopify_token, null: false
      t.timestamps
    end

    add_index :sessions, :shopify_session_id, unique: true
    add_foreign_key "users", "sessions"
  end

  def self.down
    drop_table :sessions
  end
end
